# Claude Code Instructions

Read and follow `AGENTS.md` as the repository-wide source of truth.

Launch the primary session with:

```bash
claude --agent worldwide-marketing-orchestrator
```

The orchestrator should prefer the three division leads and use specialists only for bounded work. Parallel write tasks require isolated branches or worktrees and non-overlapping file ownership.
